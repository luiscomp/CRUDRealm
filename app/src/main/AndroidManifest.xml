<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.admed.crudrealm">

    <application
        android:name=".application.CRUDRealm"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.CrudAlunosActivity"
            android:label="@string/title_activity_crud_alunos"
            android:parentActivityName=".activities.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.admed.crudrealm.activities.MainActivity" />
        </activity>
        <activity
            android:name=".activities.CrudProfessoresActivity"
            android:label="@string/title_activity_crud_professores"
            android:parentActivityName=".activities.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.admed.crudrealm.activities.MainActivity" />
        </activity>
        <activity
            android:name=".activities.CrudCursosActivity"
            android:label="@string/title_activity_crud_cursos"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activities.DetalhesCursoActivity"></activity>
    </application>

</manifest>